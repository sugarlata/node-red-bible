const DOWNLOADABLE_BIBLE_VERSION = "WEB";

// const ERROR_BOOK_EMPTY = `${ERROR_PREFIX} - 'book' argument must be a non-empty string`;
// const ERROR_CHAPTER_NOT_NUMBER = `${ERROR_PREFIX} - 'chapter' argument must be a number`;
// const ERROR_VERSION_EMPTY = `${ERROR_PREFIX} - 'version' argument must be a non-empty string`;
// const ERROR_INVALID_PARSER_FORMAT = `${ERROR_PREFIX} - 'format' argument is invalid (valid formats: ${Object.keys(bibleParser.contentParsers).join(", ")})`

// const REQUEST_ERROR = `${ERROR_PREFIX} - Web Request Error`;

module.exports = {
    // ERROR_BOOK_EMPTY,
    // ERROR_CHAPTER_NOT_NUMBER,
    // ERROR_VERSION_EMPTY,
    // ERROR_INVALID_PARSER_FORMAT,
    // REQUEST_ERROR,
    DOWNLOADABLE_BIBLE_VERSION
}